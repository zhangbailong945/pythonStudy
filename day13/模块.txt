模块
1.python解释器退出再进入，那么你定义的所有方法和变量就都消失了。python提供了一个办法，把这些定义放在文件中，为一些脚本或者交互式的解释器实例使用，这个文件成为模块。
2.python提供了一个办法，把这些定义放在文件中
3.import 语句
import somesodule
from ...import 
4.深入模块
(1))、__name__属性
一个模块被另一个程序第一次引入时，其主程序将运行。如果我们想在模块被引入时，模块中的某一程序块不执行，我们可以用__name__属性来使程序块仅在该模块自身运行时执行。
（2）、dir()函数
内置的函数dir()可以找到模块内定义的所有名称.
['__displayhook__', '__doc__', '__excepthook__', '__interactiv

3.标准模块
python本身带着一些标准的模块库。

4.包
包是一种管理python模块命名空间的形式，采用点模块名称
比如：一个模块的名称A.B，那么它表示一个包A中的子模块B。

